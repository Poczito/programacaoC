#include <stdio.h>

int main(){

    /*
    //Estrutura If >> if(condição){comando a ser executado caso condição verdadeira}
    char nome[50];
    int idade;

    printf("Digite o seu nome: ");
    scanf("%s", &nome);
    printf("Digite sua idade: ");
    scanf("%d", &idade);
    if (idade >= 18){
        printf("Você é maior de idade!\n");
    }

    */
   
    /*

    //Exemplo de estrutura if-else
    char nome[50];
    float nota;

    printf("Insira o nome do aluno: ");
    scanf("%s", &nome);
    printf("Insira a nota do aluno: ");
    scanf("%f", &nota);
    if ((float) nota >= 6)
    {
        printf("O aluno %s passou com nota %.2f", nome, nota);
    } else {
        printf("O aluno %s não passou, pois obteve nota menor que 6.0", nome);
    }
    */

    return 0;
}